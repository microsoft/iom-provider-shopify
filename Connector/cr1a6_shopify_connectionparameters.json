{
    "api_key": {
        "type": "securestring",
        "uiDefinition": {
            "displayName": "API Key",
            "description": "The access token for this api",
            "tooltip": "Provide the access token from Shopify Admin",
            "constraints": {
                "tabIndex": 2,
                "clearText": true,
                "required": "true"
            }
        }
    },
    "storeurl": {
        "type": "string",
        "uiDefinition": {
            "displayName": "Store URL",
            "description": "Shopify store url",
            "tooltip": "Provide your Shopify store url.",
            "constraints": {
                "tabIndex": 4,
                "required": "true"
            }
        }
    }
}